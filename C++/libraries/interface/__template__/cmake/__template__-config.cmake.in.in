@PACKAGE_INIT@

# Helper macro to import the interface target
macro(import_interface_target)
    if(NOT EXISTS "${CMAKE_CURRENT_LIST_DIR}/@NAME@-interface-targets.cmake")
        set(${CMAKE_FIND_PACKAGE_NAME}_NOT_FOUND_MESSAGE "@NAME@ static libraries were requested but not found")
        set(${CMAKE_FIND_PACKAGE_NAME}_FOUND OFF)
        return()
    endif()

    include("${CMAKE_CURRENT_LIST_DIR}/@NAME@-interface-targets.cmake")
endmacro()

# Only import if not already defined
if(NOT TARGET @NAMESPACE@::@NAME@)
    import_interface_target()
    check_required_components(@NAME@)
    message("-- Found interface @NAME@ (version ${${CMAKE_FIND_PACKAGE_NAME}_VERSION})")
endif()
